<Window x:Class="SupermarketPL.Views.AddGoodView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AddGoodView" Height="110" Width="180" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Button Content="-" Grid.Row="0" Grid.Column="0" Click="MinusButton_Click" Width="50" Height="50"/>
        <TextBox x:Name="counterTextBox" Grid.Row="0" Grid.Column="1" Text="0" HorizontalAlignment="Center" Width="60" 
                 FontFamily="Arial" FontSize="40" FontWeight="Bold"/>        <Button Content="+" Grid.Row="0" Grid.Column="2" Click="PlusButton_Click" Width="50" Height="50"/>
        <Button Content="Add" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Click="AddButton_Click"/>
    </Grid>
</Window>